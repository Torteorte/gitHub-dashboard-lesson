import styled from 'styled-components';

export const StyledUserPageContainer = styled.main`
  width: 500px;
  margin: auto;
  padding: 20px 5px 10px;
  border-radius: 12px;
  -webkit-box-shadow: 0 0 15px 5px rgba(34, 60, 80, 0.2);
  -moz-box-shadow: 0 0 15px 2px rgba(34, 60, 80, 0.2);
  box-shadow: 0 0 15px 2px rgba(34, 60, 80, 0.2);
`;
